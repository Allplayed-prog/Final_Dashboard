import {Shell} from '@/components/shell';import {adminDb} from '@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const{data=[]}=await adminDb().from('sanctions').select('*').order('created_at',{ascending:false});return <Shell title="Sanktionen"><div className="card"><h2>Sanktionen</h2>{data.map((x:any)=><div className="eventRow" key={x.id}><div><b>{x.member_name||x.discord_id}</b><div className="muted">{x.reason}</div></div><span className="pill orange">{x.sanction_type}</span></div>)}</div></Shell>}