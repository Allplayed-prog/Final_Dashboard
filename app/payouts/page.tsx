import {guard} from '@/lib/guards';import {adminDb} from '@/lib/db';import {Shell} from '@/components/shell';
export default async function Page(){await guard('moderator');const {data}=await adminDb().from('payouts').select('*').eq('status','open').order('amount',{ascending:false});const rows=data??[];const sum=rows.reduce((a,x)=>a+Number(x.amount),0);return <Shell title="Auszahlungen" subtitle="Verwalte offene Beträge für deine Guild"><section className="stats"><div className="stat"><div className="statLabel">Offene Auszahlungen</div><div className="statValue">{rows.length}</div></div><div className="stat"><div className="statLabel">Gesamtsumme</div><div className="statValue">{sum.toLocaleString('de-DE')}$</div></div><div className="stat"><div className="statLabel">Durchschnitt</div><div className="statValue">{rows.length?Math.round(sum/rows.length).toLocaleString('de-DE'):0}$</div></div><div className="stat"><div className="statLabel">Höchster Betrag</div><div className="statValue">{Math.max(0,...rows.map(x=>Number(x.amount))).toLocaleString('de-DE')}$</div></div></section><section className="card"><div className="cardHead"><h2>Auszahlungsliste</h2><span className="pill blue">LIVE</span></div><table className="table"><thead><tr><th>Spieler</th><th>Betrag</th><th>Erstellt</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>&lt;@{x.discord_id}&gt;</td><td><b>{Number(x.amount).toLocaleString('de-DE')}$</b></td><td>{new Date(x.created_at).toLocaleString('de-DE')}</td><td><span className="pill green">Offen</span></td></tr>)}{!rows.length&&<tr><td colSpan={4} className="muted">Keine offenen Auszahlungen.</td></tr>}</tbody></table></section></Shell>}
