import {guard} from '@/lib/guards';import {Shell} from '@/components/shell';export default async function Page(){await guard('management');const commands=[['/setup status','Einrichtung prüfen'],['/events','Eventliste'],['/open','Anmeldung öffnen'],['/start','Event starten'],['/end','Event beenden'],['/pay add','Auszahlung hinzufügen'],['/warn add','Verwarnung vergeben'],['/logs add','Logkanal hinzufügen'],['/logs test','Parser testen'],['/panel start','Panel starten'],['/panel repair','Reparatur bestätigen'],['/blacklist add','Spieler sperren']];return <Shell title="Test-Center" subtitle="Kurze Befehle und sichere Testabläufe"><div className="notice">TEST_MODE sollte beim ersten Test aktiv sein.</div><section className="card"><div className="cardHead"><h2>Kurze Commands</h2><span className="pill blue">RC3</span></div><div className="commandGrid">{commands.map(([c,d])=><div className="command" key={c}><code>{c}</code><span>{d}</span></div>)}</div></section></Shell>}
