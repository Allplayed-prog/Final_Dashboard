import {guard} from '@/lib/guards';import {hasAccess} from '@/lib/session';import {Shell} from '@/components/shell';import EventManager from '@/components/event-manager';export default async function Page(){const session=await guard('member');return <Shell title="Event-Übersicht" subtitle="Übersicht der verfügbaren Events für diese Guild"><EventManager canEdit={hasAccess(session,'moderator')}/></Shell>}
