import {Shell} from '@/components/shell';import {adminDb} from '@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const{data=[]}=await adminDb().from('blacklist').select('*').order('created_at',{ascending:false});return <Shell title="Blacklist"><div className="card"><h2>Blacklist</h2>{data.map((x:any)=><div className="eventRow" key={x.id}><div><b>#{x.id} · {x.member_name}</b><div className="muted">{x.reason}</div></div><span className={'pill '+(x.status==='active'?'red':'')}>{x.status}</span></div>)}</div></Shell>}